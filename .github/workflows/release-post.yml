name: Post on Release

on:
  release:
    types: [published]

jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - name: Tweet new release
        uses: JasonEtco/tweet@v1
        env:
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET_KEY: ${{ secrets.TWITTER_API_SECRET_KEY }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        with:
          status: "🚀 create-nttb ${{ github.event.release.tag_name }} is live! Check it out: https://github.com/SamNewhouse/create-nttb/releases/tag/${{ github.event.release.tag_name }}"
