on: [pull_request]

name: Check version

jobs:
  check:
    name: Check version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check if version changed
        uses: rpiambulance/action-pr-version-check@v0.1.1
